# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ... sauf le gabarit documenté (Docker, Tâche 4) : aucun secret réel dedans,
# doit être commité pour que quiconque clone le repo sache quelles
# variables définir.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma
graphify-out/

#own file
audit_pointage.md 
audit_module_pointage.md
src/app/(dashboard)/pointage/ticket.txt

# stockage local des pièces jointes (Demande/DepenseLigne) — jamais commité,
# correspond au volume Docker nommé "uploads" monté sur /app/uploads (voir
# docker-compose.yml/Dockerfile, DEPLOIEMENT.md)
/uploads/
audit_pointage.md
audit_module_pointage.md
cahier_de_charge_pointage.md